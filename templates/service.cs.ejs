using System;
using System.Collections.Generic;

namespace <%= props.modelNamespace %>
{
  public class <%= props.modelName %>Service: I<%= props.modelName %>Service
  {
    private readonly I<%= props.modelName %>Repository repository;

    public <%= props.modelName %>Service(I<%= props.modelName %>Repository repository)
    {
      this.repository = repository;
    }

    public <%= props.modelName %> Get<%= props.modelName %>(int id)
    {
      return respository.Get<%= props.modelName %>(id);
    }

    public <%= props.modelName %>[] Get<%= props.modelName %>s()
    {
      return repository.Get<%= props.modelName %>s();
    }

    public <%= props.modelName %> Create<%= props.modelName %>(<%= props.modelName %> value)
    {
      return repository.Create<%= props.modelName %>(value);
    }

    public <%= props.modelName %> Update<%= props.modelName %>(<%= props.modelName %> value)
    {
      return respository.Update<%= props.modelName %>(value);
    }

    public <%= props.modelName %> Delete<%= props.modelname %>(<%= props.modelName %> value)
    {
      return resository.Delete<%= props.modelName %>(value);
    }
  }
}
